# Asset Specs - 13 Layered Base 64

Create:

- `secretencodedstring.txt` where the string is encoded 5 times into base 64.

Notes:

- Make sure it works in every common base 64 decoder
- To encode 5 times, encode the original flag, then encode the output, etc.
