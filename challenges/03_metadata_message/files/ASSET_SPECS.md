# Asset Specs - 03 Metadata Message

Create:

- `school_photo.jpg` with the flag stored in a metadata field.

Notes:

- Use a common metadata field that appears on Windows.
- Verify metadata survives the upload pipeline.
