# Asset Specs - 16 Base Conversion

Create:

- `baseconversion` with numbers and when decoded into their respective bases and added then turned into binary results in the flag
Notes:

- Flag is in form BASIS{binarysum}
